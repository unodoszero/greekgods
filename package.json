{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "concurrently -c \"#93c5fd,#c4b5fd\" \"php artisan serve --host=127.0.0.1\" \"vite --host 127.0.0.1\" --names=server,vite --kill-others",
        "test:calculator": "node --test tests/js/calculator.test.mjs",
        "vite": "vite --host 127.0.0.1"
    },
    "devDependencies": {
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.1",
        "vite": "^8.0.0"
    }
}
